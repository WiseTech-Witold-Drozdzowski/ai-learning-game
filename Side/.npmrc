# Resilient install settings — the registry occasionally resets large parallel
# downloads on this network; retry and cap concurrency instead of failing.
fetch-retries=5
fetch-retry-factor=4
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
maxsockets=3
