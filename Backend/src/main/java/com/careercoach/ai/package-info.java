/**
 * Module {@code ai} — SKELETON (empty, to be implemented).
 *
 * <p>PORTS: OpenRouterClient (chat/SSE) AND ClaudeCliClient (agent) — explicitly separate. TECHNICAL_DESIGN §3.
 */
package com.careercoach.ai;
